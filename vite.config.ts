import { defineConfig } from 'vite'

// Served as a GitHub Pages project site (jxstin-potter.github.io/Limprimerie-Bakery-Website/),
// not from a domain root, so every asset URL needs this prefix.
export default defineConfig({
  base: '/Limprimerie-Bakery-Website/',
})
